# This is only needed for compatibility with Ubuntu 22, newer systems use the
# pyproject.toml
[metadata]
name = perfact-dbschematools

[options]
use_scm_version = True
setup_requires =
    setuptools_scm
