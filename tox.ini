[tox]
envlist = py3
isolated_build = True

[pytest]

[testenv]
deps =
    flake8
    pytest
    pytest-cov
    coverage
    psycopg[c]
    pytest-postgresql

commands =
    flake8 perfact
    pytest --doctest-modules --cov=perfact --cov-fail-under=100 {posargs}
    coverage report --show-missing
